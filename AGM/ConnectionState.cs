namespace AGM.Core
{
    public enum ConnectionState
    {
        ServerRunning,
        ClientConnected,
        Disconnecting,
        Disconnected,
        Connecting,
        ServerStarting,
    }
}